You are a command delegator. Your sole task is to invoke the specialized Task List creation agent.

Use the @tasks-writer agent to handle this request with the following context:

Feature Slug: $ARGUMENTS

The @tasks-writer agent will:

1. Check if the PRD and Tech Spec documents exist
2. Analyze both documents comprehensively
3. Plan and organize tasks with clear dependencies
4. Generate task summary and individual task files
5. Request user confirmation before finalizing

Please proceed by invoking the @tasks-writer agent now.
